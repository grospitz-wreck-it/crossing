import { randomUUID } from "crypto";
import { db } from "../../../lib/db";
import { OpenLocationCode } from "open-location-code";

type Point = { lat: number; lon: number };
type Station = { eva: string; stationName: string; ril100?: string; ibnr?: string; lat?: number; lon?: number; distanceKm?: number; trackDistanceMeters?: number; role?: "observation" | "context"; source?: "osm" | "catalog" };
type RailwayRoute = { ref?: string; name?: string; from?: string; to?: string; wayId?: number | null; relationId?: number | null; lineRelations?: Array<{ ref?: string; name?: string; id?: number | null }>; segments?: Point[][] };
type OpenLocationCodeRuntime = { isValid(code: string): boolean; isFull(code: string): boolean; isShort(code: string): boolean; decode(code: string): { latitudeCenter: number; longitudeCenter: number }; recoverNearest(code: string, latitude: number, longitude: number): string };
const OLC = new OpenLocationCode() as unknown as OpenLocationCodeRuntime;
function distanceKm(lat1:number,lon1:number,lat2:number,lon2:number){const r=6371,dLat=((lat2-lat1)*Math.PI)/180,dLon=((lon2-lon1)*Math.PI)/180,a=Math.sin(dLat/2)**2+Math.cos(lat1*Math.PI/180)*Math.cos(lat2*Math.PI/180)*Math.sin(dLon/2)**2;return r*2*Math.atan2(Math.sqrt(a),Math.sqrt(1-a));}
function pointSegmentDistanceMeters(lat:number,lon:number,a:Point,b:Point){const scale=111320,x=(lon-a.lon)*scale*Math.cos(lat*Math.PI/180),y=(lat-a.lat)*scale,bx=(b.lon-a.lon)*scale*Math.cos(lat*Math.PI/180),by=(b.lat-a.lat)*scale,denom=bx*bx+by*by;let t=denom?(x*bx+y*by)/denom:0;t=Math.max(0,Math.min(1,t));return Math.hypot(x-bx*t,y-by*t);}
function geometryDistanceMeters(point:Point,segments:Point[][]){let best=Infinity;for(const g of segments)for(let i=1;i<g.length;i++)best=Math.min(best,pointSegmentDistanceMeters(point.lat,point.lon,g[i-1],g[i]));return best;}
function routeBBox(routeSegments:Point[][],lat:number,lon:number,radiusKm:number){const points=routeSegments.flat(),all=points.length?points:[{lat,lon}],lats=all.map(p=>p.lat),lons=all.map(p=>p.lon),padLat=radiusKm/111,padLon=radiusKm/(111*Math.max(Math.cos(lat*Math.PI/180),.1));return{south:Math.max(-90,Math.min(...lats)-padLat),west:Math.max(-180,Math.min(...lons)-padLon),north:Math.min(90,Math.max(...lats)+padLat),east:Math.min(180,Math.max(...lons)+padLon)};}
function parseCoordinates(value:string){const normalized=value.trim().replace(/\s*[,;]\s*/g," ").replace(/\s+/g," "),m=normalized.match(/(-?\d+(?:\.\d+)?)\s+(-?\d+(?:\.\d+)?)/);if(!m)return null;const lat=Number(m[1]),lon=Number(m[2]);return Number.isFinite(lat)&&Number.isFinite(lon)&&Math.abs(lat)<=90&&Math.abs(lon)<=180?{lat,lon,source:"coordinates" as const}:null;}
function splitPlusCodeInput(value:string){const input=value.trim().replace(/^plus\s*code\s*[:：]?\s*/i,"").replace(/\s+/g," "),i=input.indexOf("+");if(i<0)return null;const left=input.slice(0,i).trim().toUpperCase(),parts=input.slice(i+1).trim().split(/\s+/),right=parts.shift();return left&&right?{code:`${left}+${right.toUpperCase()}`,locality:parts.join(" ").trim()}:null;}
async function googleGeocode(address:string){const key=process.env.GOOGLE_MAPS_API_KEY;if(!key)return null;try{const params=new URLSearchParams({address,key,language:"de",region:"de"}),r=await fetch(`https://maps.googleapis.com/maps/api/geocode/json?${params}`,{cache:"no-store"}),d=await r.json(),loc=d?.results?.[0]?.geometry?.location,lat=Number(loc?.lat),lon=Number(loc?.lng);if(d?.status!=="OK"||!Number.isFinite(lat)||!Number.isFinite(lon))return null;return{lat,lon,source:"google-geocoding" as const,globalCode:String(d?.results?.[0]?.plus_code?.global_code||"")};}catch{return null;}}
async function geocodeLocality(locality:string){if(!locality)return null;for(const q of [`${locality}, Deutschland`,locality]){try{const r=await fetch(`https://nominatim.openstreetmap.org/search?format=jsonv2&limit=1&countrycodes=de&q=${encodeURIComponent(q)}`,{headers:{"User-Agent":"Crossings/1.0 (meineschranke.com)"},cache:"no-store"}),d=await r.json(),x=d?.[0],lat=Number(x?.lat),lon=Number(x?.lon);if(Number.isFinite(lat)&&Number.isFinite(lon))return{lat,lon,source:"geocoder" as const};}catch{}}return null;}
async function stationReference(locality:string){if(!locality)return null;try{const r=await db.execute({sql:`SELECT eva,name,lat,lon FROM railway_station_catalog WHERE lat IS NOT NULL AND lon IS NOT NULL AND name LIKE ? COLLATE NOCASE ORDER BY length(name) LIMIT 1`,args:[`%${locality}%`]}),x:any=r.rows[0],lat=Number(x?.lat),lon=Number(x?.lon);return Number.isFinite(lat)&&Number.isFinite(lon)?{lat,lon,station:String(x?.name||x?.eva||locality)}:null;}catch{return null;}}
async function resolveLocation(value:string){const coordinates=parseCoordinates(value);if(coordinates)return{result:coordinates,debug:{stage:"coordinates"}};const parsed=splitPlusCodeInput(value);if(!parsed)return{result:null,debug:{stage:"parse",reason:"invalid-plus-code-input"}};const{code,locality}=parsed;try{if(OLC.isFull(code)){const d=OLC.decode(code);return{result:{lat:d.latitudeCenter,lon:d.longitudeCenter,source:"plus-code" as const},debug:{stage:"decode-full",code}};}if(!OLC.isShort(code)||!locality)return{result:null,debug:{stage:"parse",code,locality,isValid:OLC.isValid(code)}};const google=await googleGeocode(`${code} ${locality}`);if(google)return{result:google,debug:{stage:"google-geocoding",code,locality}};const reference=await stationReference(locality)||await geocodeLocality(locality);if(!reference)return{result:null,debug:{stage:"reference-resolution",reference:false,code,locality}};const recovered=OLC.recoverNearest(code,reference.lat,reference.lon),d=OLC.decode(recovered);return{result:{lat:d.latitudeCenter,lon:d.longitudeCenter,source:"plus-code-recovered" as const},debug:{stage:"recover-nearest",code,locality,recovered,reference}};}catch(error){return{result:null,debug:{stage:"resolve-exception",error:error instanceof Error?error.message:String(error),code,locality}};}}
async function expandRouteSegments(route:RailwayRoute|null):Promise<Point[][]>{if(!route)return[];if(route.relationId){for(const endpoint of ["https://overpass-api.de/api/interpreter","https://overpass.kumi.systems/api/interpreter"]){try{const q=`[out:json][timeout:25];rel(${Number(route.relationId)});(._;>;);out geom;`,r=await fetch(`${endpoint}?${new URLSearchParams({data:q})}`,{cache:"no-store",headers:{accept:"application/json","user-agent":"Crossings/1.0 (meineschranke.com)"}});if(!r.ok)continue;const d=await r.json(),segments=(d?.elements||[]).filter((e:any)=>e.type==="way"&&e.tags?.railway==="rail"&&Array.isArray(e.geometry)).map((e:any)=>e.geometry.map((p:any)=>({lat:Number(p.lat),lon:Number(p.lon)})).filter((p:Point)=>Number.isFinite(p.lat)&&Number.isFinite(p.lon))).filter((g:Point[])=>g.length>=2);if(segments.length)return segments;}catch{}}}return Array.isArray(route.segments)?route.segments:[];}
async function loadCatalogForRoute(routeSegments:Point[][],lat:number,lon:number):Promise<Station[]>{const bbox=routeBBox(routeSegments,lat,lon,80);try{const r=await db.execute({sql:`SELECT eva,name,lat,lon,ril100,ibnr FROM railway_station_catalog WHERE lat BETWEEN ? AND ? AND lon BETWEEN ? AND ?`,args:[bbox.south,bbox.north,bbox.west,bbox.east]});return r.rows.map((x:any)=>({eva:String(x.eva||""),stationName:String(x.name||x.eva||""),ril100:String(x.ril100||""),ibnr:String(x.ibnr||x.eva||""),lat:Number(x.lat),lon:Number(x.lon),source:"catalog" as const})).filter((s:any)=>s.eva&&Number.isFinite(s.lat)&&Number.isFinite(s.lon));}catch{return[];}}
function isLargeStation(name:string){const n=name.toLowerCase();return /\bhbf\b|hauptbahnhof|zentralbahnhof/.test(n);}
function uniqueStations(stations:Station[]){const seen=new Set<string>();return stations.filter(s=>{const k=s.eva||`${s.stationName}|${Number(s.lat).toFixed(4)}|${Number(s.lon).toFixed(4)}`;if(seen.has(k))return false;seen.add(k);return true;});}
async function derivePredictionConfig(lat:number,lon:number,route:RailwayRoute|null){try{const segments=await expandRouteSegments(route),catalog=await loadCatalogForRoute(segments,lat,lon);if(!catalog.length)return{observationEvas:[],contextEvas:[],requiredRouteStops:[],throughRules:[],diversionRules:[],stations:[] as Station[],osmStations:[] as Station[]};const onRoute=catalog.map(s=>({...s,trackDistanceMeters:segments.length?geometryDistanceMeters({lat:Number(s.lat),lon:Number(s.lon)},segments):Infinity,distanceKm:distanceKm(lat,lon,Number(s.lat),Number(s.lon))})).filter(s=>Number(s.trackDistanceMeters)<=2500&&Number(s.distanceKm)<=75).sort((a,b)=>Number(a.distanceKm)-Number(b.distanceKm));const observation=uniqueStations(onRoute).slice(0,8).map(s=>({...s,role:"observation" as const}));const observationSet=new Set(observation.map(s=>s.eva));const context=uniqueStations(catalog.map(s=>({...s,distanceKm:distanceKm(lat,lon,Number(s.lat),Number(s.lon))})).filter(s=>Number(s.distanceKm)<=75&&isLargeStation(s.stationName)&&!observationSet.has(s.eva)).sort((a,b)=>Number(a.distanceKm)-Number(b.distanceKm)).slice(0,6)).map(s=>({...s,role:"context" as const,trackDistanceMeters:geometryDistanceMeters({lat:Number(s.lat),lon:Number(s.lon)},segments)}));const routeStops=Array.from(new Set([route?.from,route?.to].filter((v):v is string=>Boolean(v?.trim()))));const requiredRouteStops=routeStops.length?routeStops:observation.slice(0,2).map(s=>s.stationName);const sources=[...observation,...context];const throughRules=sources.filter(s=>s.eva).map(s=>({observationEva:s.eva,observationStation:s.stationName,categories:["ICE","IC","EC"],trackDistanceMeters:Math.round(Number(s.trackDistanceMeters||0)),fallbackOffsetSeconds:Math.max(60,Math.round((Math.max(.5,Number(s.distanceKm||0))/90)*3600)),direction:"unknown" as const}));const diversionRules=context.length&&requiredRouteStops.length?context.map(s=>({observationEva:s.eva,observationStation:s.stationName,categories:["ICE","IC","EC"],anchorRouteStops:requiredRouteStops.slice(0,2),excludedRouteStop:observation[0]?.stationName||requiredRouteStops[0]})):[];return{observationEvas:observation.map(s=>s.eva),contextEvas:context.map(s=>s.eva),requiredRouteStops,throughRules,diversionRules,stations:sources,osmStations:sources};}catch(error){console.error("derivePredictionConfig failed",error);return{observationEvas:[],contextEvas:[],requiredRouteStops:[],throughRules:[],diversionRules:[],stations:[] as Station[],osmStations:[] as Station[]};}}
async function discoverStations(lat:number,lon:number){try{const catalog=await loadCatalogForRoute([],lat,lon);return uniqueStations(catalog.map(s=>({...s,distanceKm:distanceKm(lat,lon,Number(s.lat),Number(s.lon))})).filter(s=>Number(s.distanceKm)<=25).sort((a,b)=>Number(a.distanceKm)-Number(b.distanceKm)).slice(0,12));}catch{return[];}}
function selectedOsmRoute(route: RailwayRoute|null){if(!route)return null;const lineRefs=Array.from(new Set((route.lineRelations||[]).map((relation)=>String(relation?.ref||"").trim()).filter(Boolean)));const fingerprint={wayId:route.wayId??null,relationId:route.relationId??null,ref:String(route.ref||"").trim(),name:String(route.name||"").trim(),from:String(route.from||"").trim(),to:String(route.to||"").trim(),lineRefs};return Object.values(fingerprint).some((value)=>Array.isArray(value)?value.length>0:Boolean(value))?fingerprint:null;}
async function loadCrossings(){const r=await db.execute(`SELECT id,name,eva,lat,lon,confidence,status,source,observation_evas,context_evas,required_route_stops,close_offset_seconds,open_offset_seconds,created_at,updated_at FROM crossings ORDER BY name COLLATE NOCASE`);return r.rows.map((row:any)=>{let observationEvas:string[]=[],contextEvas:string[]=[];try{observationEvas=JSON.parse(String(row.observation_evas||"[]"));}catch{}try{contextEvas=JSON.parse(String(row.context_evas||"[]"));}catch{}return{...row,stations:[...observationEvas.map(eva=>({eva,stationName:eva,role:"observation"})),...contextEvas.map(eva=>({eva,stationName:eva,role:"context"}))]};});}
export async function GET(request:Request){const{searchParams}=new URL(request.url),locationInput=searchParams.get("location")||searchParams.get("coordinates"),rows=await loadCrossings();if(!locationInput)return Response.json(rows);const resolved=await resolveLocation(locationInput);if(!resolved.result)return Response.json({error:"Standort konnte nicht erkannt werden. Bitte Google-Maps-Plus-Code, Koordinaten oder einen vollständigen Plus Code eingeben.",debug:{input:locationInput,parsed:splitPlusCodeInput(locationInput),...resolved.debug,googleGeocodingConfigured:Boolean(process.env.GOOGLE_MAPS_API_KEY)}},{status:400});const coords=resolved.result,nearest=rows.map((row:any)=>({...row,distanceKm:distanceKm(coords.lat,coords.lon,Number(row.lat),Number(row.lon))})).sort((a:any,b:any)=>a.distanceKm-b.distanceKm).slice(0,5);return Response.json({input:locationInput,location:coords,debug:resolved.debug,nearest,stations:await discoverStations(coords.lat,coords.lon)});}
export async function POST(request:Request){let stage="request";try{const body=await request.json(),suppliedName=String(body.name||"").trim(),routeRef=String(body.routeRef||body.selectedRouteRef||"").trim(),routeName=String(body.routeName||body.selectedRouteName||"").trim(),name=suppliedName||["Bahnübergang",routeRef?`Strecke ${routeRef}`:routeName].filter(Boolean).join(" "),eva=String(body.eva||"").trim(),lat=Number(body.lat),lon=Number(body.lon),route:RailwayRoute|null=body.selectedRoute&&typeof body.selectedRoute==="object"?body.selectedRoute:null;if(!name)return Response.json({error:"Bitte einen Namen für den Bahnübergang vergeben.",stage:"validation"},{status:400});if(!Number.isFinite(lat)||!Number.isFinite(lon))return Response.json({error:"Standortkoordinaten fehlen. Bitte zuerst den Plus Code prüfen.",stage:"validation"},{status:400});stage="duplicate-name";const sameName=await db.execute({sql:`SELECT id,name,lat,lon FROM crossings WHERE LOWER(TRIM(name))=LOWER(TRIM(?)) LIMIT 1`,args:[name]});if(sameName.rows.length)return Response.json({error:`Dieser Name wird bereits verwendet: „${String((sameName.rows[0] as any).name)}“.`,code:"DUPLICATE_NAME",existing:sameName.rows[0]},{status:409});stage="duplicate-location";const sameLocation=await db.execute({sql:`SELECT id,name,lat,lon FROM crossings WHERE ROUND(lat,5)=ROUND(?,5) AND ROUND(lon,5)=ROUND(?,5) LIMIT 1`,args:[lat,lon]});if(sameLocation.rows.length)return Response.json({error:`An diesem Standort existiert bereits der Bahnübergang „${String((sameLocation.rows[0] as any).name)}“.`,code:"DUPLICATE_LOCATION",existing:sameLocation.rows[0]},{status:409});stage="derive-prediction-config";const derived=await derivePredictionConfig(lat,lon,route),osmRoute=selectedOsmRoute(route),throughRules=derived.throughRules.map((rule:any)=>osmRoute?{...rule,osmRoute}:rule),requiredRouteStops=derived.requiredRouteStops.length?derived.requiredRouteStops:(routeRef?[routeRef]:[]),idBase=name.toLowerCase().normalize("NFKD").replace(/[\u0300-\u036f]/g,"").replace(/[^a-z0-9]+/g,"-").replace(/^-|-$/g,"")||"crossing",id=`${idBase}-${randomUUID().slice(0,8)}`;stage="insert-crossing";await db.execute({sql:`INSERT INTO crossings (id,name,eva,observation_evas,context_evas,required_route_stops,lat,lon,close_offset_seconds,open_offset_seconds,rules,through_rules,diversion_rules,reroute_watch_rules,confidence,source,status) VALUES (?,?,?,?,?,?,?,?,?,?,'[]',?,?,?,?,'manual','active')`,args:[id,name,eva,JSON.stringify(derived.observationEvas),JSON.stringify(derived.contextEvas),JSON.stringify(requiredRouteStops),lat,lon,Number(body.closeOffsetSeconds??80),Number(body.openOffsetSeconds??20),JSON.stringify(throughRules),JSON.stringify(derived.diversionRules),JSON.stringify([]),Number(body.confidence??0.5)]});stage="verify";const result=await db.execute({sql:`SELECT id,name,eva,lat,lon,confidence,status,source,observation_evas,context_evas,required_route_stops,close_offset_seconds,open_offset_seconds,created_at,updated_at FROM crossings WHERE id=? LIMIT 1`,args:[id]});if(!result.rows.length)return Response.json({error:"Der Übergang wurde gespeichert, konnte aber nicht bestätigt werden.",stage},{status:500});return Response.json({ok:true,id,crossing:result.rows[0],autoStations:derived.osmStations,autoRules:{requiredRouteStops,throughRules,diversionRules:derived.diversionRules},osmRoute},{status:201});}catch(error){const message=error instanceof Error?error.message:String(error);if(/DUPLICATE_NAME/i.test(message))return Response.json({error:"Dieser Name wird bereits verwendet.",code:"DUPLICATE_NAME",stage},{status:409});if(/DUPLICATE_LOCATION/i.test(message))return Response.json({error:"Dieser Standort wird bereits verwendet.",code:"DUPLICATE_LOCATION",stage},{status:409});if(/UNIQUE|constraint/i.test(message))return Response.json({error:"Dieser Bahnübergang existiert bereits. Bitte prüfe Name und Standort.",code:"DUPLICATE",stage},{status:409});return Response.json({error:message||"Speichern fehlgeschlagen.",stage},{status:500});}}
export async function DELETE(request:Request){const id=String(new URL(request.url).searchParams.get("id")||"").trim();if(!id)return Response.json({error:"ID des Bahnübergangs fehlt."},{status:400});try{const existing=await db.execute({sql:`SELECT id,name FROM crossings WHERE id=? LIMIT 1`,args:[id]});if(!existing.rows.length)return Response.json({error:"Bahnübergang nicht gefunden.",code:"NOT_FOUND"},{status:404});for(const table of ["user_crossings","campaign_crossings"]){try{await db.execute({sql:`DELETE FROM ${table} WHERE crossing_id=?`,args:[id]});}catch(error){console.warn(`DELETE ${table} skipped`,error);}}await db.execute({sql:`DELETE FROM crossings WHERE id=?`,args:[id]});const verify=await db.execute({sql:`SELECT id FROM crossings WHERE id=? LIMIT 1`,args:[id]});if(verify.rows.length)return Response.json({error:"Der Bahnübergang konnte nicht vollständig gelöscht werden."},{status:500});return Response.json({ok:true,id});}catch(error){return Response.json({error:error instanceof Error?error.message:String(error)},{status:500});}}