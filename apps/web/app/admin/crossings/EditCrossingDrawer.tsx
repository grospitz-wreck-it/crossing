"use client";

import { useEffect, useMemo, useState } from "react";
import type { CSSProperties } from "react";

type StationLink = { id?: string; eva: string; station_name: string; role: string; categories: string; direction: string; fallback_offset_seconds: number; track_distance_meters: number; };
type Props = { crossingId: string; onClose: () => void; onSaved?: () => void };

const inputStyle: CSSProperties = { width: "100%", boxSizing: "border-box", border: "1px solid #dfe3e8", borderRadius: 10, padding: "11px 12px", font: "inherit", fontSize: 13 };
const labelStyle: CSSProperties = { fontSize: 11, fontWeight: 800, color: "#343c46" };

export default function EditCrossingDrawer({ crossingId, onClose, onSaved }: Props) {
  const [loading, setLoading] = useState(true); const [saving, setSaving] = useState(false); const [error, setError] = useState("");
  const [form, setForm] = useState({ name: "", eva: "", lat: "", lon: "", closeOffsetSeconds: "80", openOffsetSeconds: "20", confidence: "0.5", status: "active" });
  const [routeStops, setRouteStops] = useState<string[]>([]); const [links, setLinks] = useState<StationLink[]>([]);
  useEffect(() => { let alive = true; (async () => { try { const res = await fetch(`/api/admin/crossings/${encodeURIComponent(crossingId)}`, { cache: "no-store" }); const data = await res.json().catch(() => ({})); if (!res.ok) throw new Error(data.error || "Übergang konnte nicht geladen werden."); if (!alive) return; const c = data.crossing || {}; setForm({ name: String(c.name || ""), eva: String(c.eva || ""), lat: String(c.lat ?? ""), lon: String(c.lon ?? ""), closeOffsetSeconds: String(c.close_offset_seconds ?? 80), openOffsetSeconds: String(c.open_offset_seconds ?? 20), confidence: String(c.confidence ?? 0.5), status: String(c.status || "active") }); setRouteStops(Array.isArray(c.requiredRouteStops) ? c.requiredRouteStops : []); setLinks(Array.isArray(data.stationLinks) ? data.stationLinks : []); } catch (e) { if (alive) setError(e instanceof Error ? e.message : "Laden fehlgeschlagen."); } finally { if (alive) setLoading(false); } })(); return () => { alive = false; }; }, [crossingId]);
  const mapSrc = useMemo(() => { const lat = Number(form.lat), lon = Number(form.lon); if (!Number.isFinite(lat) || !Number.isFinite(lon)) return ""; const d = 0.006; return `https://www.openstreetmap.org/export/embed.html?bbox=${lon-d},${lat-d},${lon+d},${lat+d}&layer=mapnik&marker=${lat},${lon}`; }, [form.lat, form.lon]);
  async function save() { setSaving(true); setError(""); try { const res = await fetch(`/api/admin/crossings/${encodeURIComponent(crossingId)}`, { method: "PATCH", headers: { "content-type": "application/json" }, body: JSON.stringify({ name: form.name, eva: form.eva, lat: Number(form.lat), lon: Number(form.lon), closeOffsetSeconds: Number(form.closeOffsetSeconds), openOffsetSeconds: Number(form.openOffsetSeconds), confidence: Number(form.confidence), status: form.status, requiredRouteStops: routeStops, stationLinks: links }) }); const data = await res.json().catch(() => ({})); if (!res.ok) throw new Error(data.error || "Speichern fehlgeschlagen."); onSaved?.(); onClose(); } catch (e) { setError(e instanceof Error ? e.message : "Speichern fehlgeschlagen."); } finally { setSaving(false); } }
  function setField(key: keyof typeof form, value: string) { setForm((current) => ({ ...current, [key]: value })); }
  function addStop() { setRouteStops((items) => [...items, ""]); }
  function addStation() { setLinks((items) => [...items, { eva: "", station_name: "", role: "observation", categories: "ICE,IC,EC,RE,RB", direction: "unknown", fallback_offset_seconds: 0, track_distance_meters: 0 }]); }
  return <div style={{ position: "fixed", inset: 0, zIndex: 420, background: "rgba(15,20,28,.42)", display: "flex", justifyContent: "flex-end" }} onMouseDown={(e) => e.target === e.currentTarget && !saving && onClose()}><aside style={{ width: "min(760px,100vw)", height: "100%", background: "#fff", boxShadow: "-25px 0 70px rgba(0,0,0,.2)", display: "flex", flexDirection: "column" }} onMouseDown={(e) => e.stopPropagation()}><header style={{ padding: "26px 30px 20px", borderBottom: "1px solid #eceef1", display: "flex", justifyContent: "space-between", gap: 20 }}><div><div style={{ color: "#e10600", fontSize: 10, fontWeight: 850, letterSpacing: ".14em", marginBottom: 7 }}>BAHNÜBERGANG · BEARBEITEN</div><h2 style={{ margin: 0, fontSize: 30, letterSpacing: "-.035em" }}>{form.name || "Übergang"}</h2></div><button onClick={onClose} disabled={saving} style={{ border: 0, background: "#f1f2f4", borderRadius: 10, width: 38, height: 38, fontSize: 24, cursor: "pointer" }}>×</button></header>{loading ? <div style={{ padding: 30 }}>Übergang wird geladen…</div> : <><div style={{ overflow: "auto", padding: 26, display: "grid", gap: 24 }}>{error && <div style={{ padding: 12, borderRadius: 10, background: "#fff4f4", border: "1px solid #f1d5d8", color: "#b4232d", fontSize: 12 }}>{error}</div>}<section style={{ display: "grid", gap: 10 }}><strong style={{ fontSize: 13 }}>Standort</strong><div style={{ display: "grid", gridTemplateColumns: "1fr 1fr", gap: 10 }}><label style={{ display: "grid", gap: 6 }}><span style={labelStyle}>Name</span><input style={inputStyle} value={form.name} onChange={(e) => setField("name", e.target.value)} /></label><label style={{ display: "grid", gap: 6 }}><span style={labelStyle}>EVA</span><input style={inputStyle} value={form.eva} onChange={(e) => setField("eva", e.target.value)} /></label><label style={{ display: "grid", gap: 6 }}><span style={labelStyle}>Latitude</span><input style={inputStyle} value={form.lat} onChange={(e) => setField("lat", e.target.value)} /></label><label style={{ display: "grid", gap: 6 }}><span style={labelStyle}>Longitude</span><input style={inputStyle} value={form.lon} onChange={(e) => setField("lon", e.target.value)} /></label></div></section>{mapSrc && <div style={{ height: 260, overflow: "hidden", borderRadius: 14, border: "1px solid #dfe3e8" }}><iframe title="Standortkarte" src={mapSrc} style={{ width: "100%", height: "100%", border: 0 }} loading="lazy" /></div>}<section style={{ display: "grid", gap: 10 }}><strong style={{ fontSize: 13 }}>Schließberechnung</strong><div style={{ display: "grid", gridTemplateColumns: "1fr 1fr 1fr", gap: 10 }}><label style={{ display: "grid", gap: 6 }}><span style={labelStyle}>Schließ-Offset (Sek.)</span><input type="number" style={inputStyle} value={form.closeOffsetSeconds} onChange={(e) => setField("closeOffsetSeconds", e.target.value)} /></label><label style={{ display: "grid", gap: 6 }}><span style={labelStyle}>Öffnungs-Offset (Sek.)</span><input type="number" style={inputStyle} value={form.openOffsetSeconds} onChange={(e) => setField("openOffsetSeconds", e.target.value)} /></label><label style={{ display: "grid", gap: 6 }}><span style={labelStyle}>Konfidenz</span><input type="number" min="0" max="1" step="0.05" style={inputStyle} value={form.confidence} onChange={(e) => setField("confidence", e.target.value)} /></label></div></section><section style={{ display: "grid", gap: 10 }}><div style={{ display: "flex", justifyContent: "space-between", alignItems: "center" }}><strong style={{ fontSize: 13 }}>Strecken-Haltepunkte</strong><button onClick={addStop} style={{ border: 0, background: "#f1f2f4", borderRadius: 9, padding: "8px 11px", cursor: "pointer", fontWeight: 750 }}>+ Halt</button></div>{routeStops.map((stop, i) => <div key={i} style={{ display: "grid", gridTemplateColumns: "1fr 34px", gap: 8 }}><input style={inputStyle} value={stop} onChange={(e) => setRouteStops((items) => items.map((v, j) => j === i ? e.target.value : v))} placeholder="z. B. Bünde (Westf)" /><button onClick={() => setRouteStops((items) => items.filter((_, j) => j !== i))} style={{ border: 0, borderRadius: 9, background: "#fff4f4", color: "#c1121f", fontSize: 18, cursor: "pointer" }}>×</button></div>)}</section><section style={{ display: "grid", gap: 10 }}><div style={{ display: "flex", justifyContent: "space-between", alignItems: "center" }}><strong style={{ fontSize: 13 }}>DB-Beobachtungsstationen</strong><button onClick={addStation} style={{ border: 0, background: "#111827", color: "#fff", borderRadius: 9, padding: "8px 11px", cursor: "pointer", fontWeight: 750 }}>+ Station</button></div>{links.map((link, i) => <div key={`${link.id || "new"}-${i}`} style={{ padding: 12, border: "1px solid #e5e7eb", borderRadius: 11, display: "grid", gap: 8 }}><div style={{ display: "grid", gridTemplateColumns: "110px 1fr 120px", gap: 8 }}><input style={inputStyle} placeholder="EVA" value={link.eva} onChange={(e) => setLinks((items) => items.map((v,j)=>j===i?{...v,eva:e.target.value}:v))} /><input style={inputStyle} placeholder="Stationsname" value={link.station_name} onChange={(e) => setLinks((items) => items.map((v,j)=>j===i?{...v,station_name:e.target.value}:v))} /><select style={inputStyle} value={link.role} onChange={(e) => setLinks((items) => items.map((v,j)=>j===i?{...v,role:e.target.value}:v))}><option value="primary">primary</option><option value="observation">observation</option><option value="anchor">anchor</option></select></div><div style={{ display: "grid", gridTemplateColumns: "1fr 1fr 34px", gap: 8 }}><input style={inputStyle} placeholder="Kategorien, z. B. ICE,IC,RE" value={link.categories} onChange={(e) => setLinks((items) => items.map((v,j)=>j===i?{...v,categories:e.target.value}:v))} /><select style={inputStyle} value={link.direction} onChange={(e) => setLinks((items) => items.map((v,j)=>j===i?{...v,direction:e.target.value}:v))}><option value="unknown">Richtung unbekannt</option><option value="eastbound">eastbound</option><option value="westbound">westbound</option></select><button onClick={() => setLinks((items) => items.filter((_,j)=>j!==i))} style={{ border: 0, borderRadius: 9, background: "#fff4f4", color: "#c1121f", fontSize: 18, cursor: "pointer" }}>×</button></div></div>)}</section><section style={{ display: "grid", gap: 8 }}><label style={{ display: "grid", gap: 6 }}><span style={labelStyle}>Status</span><select style={inputStyle} value={form.status} onChange={(e) => setField("status", e.target.value)}><option value="active">active</option><option value="inactive">inactive</option></select></label></section></div><footer style={{ padding: "16px 26px", borderTop: "1px solid #eceef1", display: "flex", justifyContent: "flex-end", gap: 10 }}><button onClick={onClose} disabled={saving} style={{ border: 0, borderRadius: 10, padding: "12px 16px", background: "#f1f2f4", cursor: "pointer", fontWeight: 750 }}>Abbrechen</button><button onClick={() => void save()} disabled={saving} style={{ border: 0, borderRadius: 10, padding: "12px 18px", background: "#e10600", color: "#fff", cursor: "pointer", fontWeight: 800 }}>{saving ? "Speichert…" : "Änderungen speichern"}</button></footer></>}</aside></div>;
}
